{% macro user_table_row(user) %}
    <tr>
        <td>{{ user.firstName }}</td>
        <td>{{ user.middlename }}</td>
        <td>{{ user.lastName }}</td>
        <td>{{ user.phone }}</td>
    </tr>
{% endmacro %}